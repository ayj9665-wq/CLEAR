# xgboost는 한동안 뺐다가 2026-08-04에 되돌렸다. 평면 baseline
# (experiments/train_baseline.py)만 쓰던 의존이라 그 스크립트를 지우면서 같이 뺐는데,
# 전국 평면 대조가 필요해져 커밋 96d1dd1에서 스크립트를 복원했기 때문이다.
#
# 2.1.4 하한은 취향이 아니다 — 2.1.3 이하는 scikit-learn>=1.6의 __sklearn_tags__
# API와 호환되지 않아 GridSearchCV.fit()에서 죽는다.
#
# **3개 주 덤프는 재생성하지 말 것.** outputs/predictions/{logreg,xgboost}[_blind].csv
# 4개는 gitignore 예외로 커밋돼 있고 프로젝트 헤드라인(+0.82)의 근거다. 원래 그 파일을
# 만든 xgboost는 2.x였고 지금 설치본은 3.x라, 다시 돌리면 예측이 달라질 수 있다.
# 복원한 학습기는 전국 스코프에서만 돌렸다.
xgboost>=2.1.4
# scikit-learn은 분할(train_test_split)과 지표(clear.metrics)에 쓰인다.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
seaborn>=0.12
pyarrow>=14.0
# 주의: 순정 `pip install -r requirements.txt`는 torch를 CPU 전용 wheel로 설치함.
# CUDA 빌드가 필요하면 먼저 아래를 실행한 뒤 나머지를 설치할 것:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.6
torch-geometric>=2.7
# NeighborLoader(전국 확장의 미니배치 학습)에만 필요하다. torch-geometric의
# NeighborSampler는 pyg-lib 또는 torch-sparse를 요구하는데, **torch-sparse는 이
# 조합(win_amd64 / torch 2.6+cu124 / cp313)에 프리빌트 wheel이 없어** 소스 빌드로
# 떨어진다(MSVC + CUDA 툴킷 필요). pyg-lib은 wheel이 있으므로 이쪽을 쓴다.
# PyPI에는 없고 PyG 인덱스에서만 받는다:
#   pip install pyg-lib -f https://data.pyg.org/whl/torch-2.6.0+cu124.html
# full-batch(3개 주 기본 경로)만 돌린다면 없어도 된다 — clear.gnn이 loader import를
# 함수 안에 두고 있어서, --minibatch를 쓸 때만 이 의존이 걸린다.
pyg-lib; platform_system != "Windows"   # Windows는 위 인덱스에서 수동 설치
